okay, what if we take an audio, then split that audio into chunks, and those chunks are split based on where pauses accrue, then feed those pauses into a model, where that model tries to predict the next chunk that is related to the original audio, which makes training easy because it just needs an audio, then step by step, make it learn language by showing it lots of examples on it, then we make it into a native audio speaker