[build-system]
requires = [
    "setuptools>=61.0",
    "wheel",
    "pybind11>=2.10"
]
build-backend = "setuptools.build_meta"

[project]
name = "NN"
version = "0.1.0"
description = "C++ Neural Network with pybind11"
requires-python = ">=3.8"